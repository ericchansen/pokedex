---
target: "http://127.0.0.1:8148/#/boxes"
total_score: 26
p0_count: 1
p1_count: 3
timestamp: 2026-07-17T01-47-52Z
slug: 127-0-0-1
---
# Boxes Route Critique

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Collection progress is visible, but single-slot removal provides no confirmation or success feedback. |
| 2 | Match System / Real World | 4 | Boxes, presets, IVs, forms, and game terminology fit an experienced Pokémon collector. |
| 3 | User Control and Freedom | 2 | The grid has no keyboard path, and single-slot removal has no undo or confirmation. |
| 4 | Consistency and Standards | 3 | The visual system is coherent, but single and batch removal protect users differently. |
| 5 | Error Prevention | 2 | Batch removal confirms; an equally destructive single removal executes immediately. |
| 6 | Recognition Rather Than Recall | 2 | Preset and completion states depend on color and hover-only tooltips without a persistent legend. |
| 7 | Flexibility and Efficiency | 2 | Mouse power tools are strong, but the core grid has no keyboard accelerators or navigation. |
| 8 | Aesthetic and Minimalist Design | 3 | The density is purposeful, though the FLAGS group and unexplained state channels add avoidable load. |
| 9 | Error Recovery | 2 | Removal lacks undo, and one failure message directs the user to the developer console. |
| 10 | Help and Documentation | 1 | There is no in-context legend or task help for the route's state system. |
| **Total** | | **26/40** | **Acceptable — strong identity, significant interaction gaps** |

## Anti-Patterns Verdict

**LLM assessment:** This does not look AI-generated. The dark PokéPC palette, sprite-led spatial grid, completion treatment, compact chrome, and authentic alternate Gen III theme are specific to the product. The interface avoids generic SaaS composition and respects the documented “Bill's PC” identity.

**Deterministic scan:** The static detector reported 162 findings: 156 advisory token-consistency findings and 6 warnings. Most advisories are literal colors and radii in `site/css/styles.css`, useful as design-system debt but not evidence of broken visual flow. Warning-level findings include three width transitions, one side accent border, and two false positives: an `<img>` mentioned in a CSS comment and a static claim that the site uses only one font.

**Browser evidence:** Live detector injection succeeded on the Boxes route and created a visible **[Human]** browser tab with 13 overlays. The credible findings are low contrast for small white text on Link Cable Blue and Warning Red, muted text below 4.5:1, cramped progress-bar content, and unlabeled form controls. Gray-on-color, “overused Roboto,” and wide-shadow warnings require contextual review and should not drive redesign.

## Overall Impression

The Boxes route already has the right product identity and spatial model. Its biggest opportunity is not a new look; it is making the existing state system understandable and every core action reachable without weakening expert density.

## What's Working

1. Pokémon remain the dominant visual material, and the six-column box metaphor matches the user's real collection.
2. Multi-select, drag-and-drop, clipboard operations, presets, and compact indicators provide genuine expert efficiency.
3. Completion progress and documented-Pokémon treatments create an appropriate completionist reward loop without becoming a generic dashboard.

## Cognitive Load

The route has **moderate cognitive load** with two clear failures:

- **Minimal choices:** FLAGS exposes eight simultaneous checkboxes, making the densest decision group harder to scan than necessary.
- **Progressive disclosure:** Match, mismatch, owned-elsewhere, completion, and training channels have no persistent key; users must rediscover meaning through hover.

The sprite grid itself is not the problem. Its density is intrinsic to the collection task and is organized into familiar boxes.

## Emotional Journey

The peak is seeing collection progress and completed Pokémon accumulate across a recognizable HOME-like grid. The emotional valley is removal: a high-value tracked Pokémon can disappear immediately without confirmation, undo, or even a success toast. That undermines the product principle of earning trust through precision.

## Priority Issues

### P0 — The core grid is keyboard-unreachable

**Why it matters:** Slots are not in the tab order and expose no keyboard activation or focus treatment. A keyboard or screen-reader user cannot inspect, place, edit, or remove Pokémon—the route's primary task is blocked.

**Fix:** Give occupied and empty slots appropriate button/grid semantics, roving keyboard focus, visible focus treatment, Enter/Space activation, and documented shortcuts for selection and movement.

**Suggested command:** `/impeccable audit`

### P1 — Mobile filters are clipped and unreachable

**Why it matters:** At a narrow viewport, filter groups remain single-line while horizontal overflow is hidden. Later FLAGS and game filters render beyond the viewport with no scroll path.

**Fix:** Recompose the toolbar for mobile: wrap groups internally, move secondary filters into an explicit disclosure, or use a compact filter sheet while preserving active-filter visibility.

**Suggested command:** `/impeccable adapt`

### P1 — Destructive removal has inconsistent safeguards

**Why it matters:** Single-slot removal executes immediately, while batch removal confirms and reports completion. Users cannot predict the safety model, and a fast misclick can erase manually curated data.

**Fix:** Use one removal contract everywhere. Prefer an immediate action with a clear undo toast, or require confirmation consistently if restoration is not available.

**Suggested command:** `/impeccable harden`

### P1 — Semantic states rely on color and hover

**Why it matters:** Preset match, mismatch, owned-elsewhere, completion, and training states cannot be reliably decoded by touch, keyboard, or color-blind users. Unknown state can be mistaken for confirmed state.

**Fix:** Add a compact persistent legend or on-demand key, pair color with shape/icon/label, and expose equivalent accessible names and descriptions on each slot.

**Suggested command:** `/impeccable clarify`

### P2 — Small text contrast is below the intended readability floor

**Why it matters:** Muted copy and placeholders are difficult to read, while white text on the current blue and red action fills does not consistently reach 4.5:1 at control-label sizes.

**Fix:** Raise muted-text luminance and use darker text or darker action fills where small labels require AA contrast.

**Suggested command:** `/impeccable colorize`

## Persona Red Flags

**Alex, the power user:** Multi-select and clipboard operations are valuable, but the lack of grid keyboard navigation prevents a truly fast workflow. Immediate removal without undo punishes speed.

**Sam, the accessibility-dependent user:** The primary task is blocked because slots are not keyboard-focusable. Hover-only explanations, color-only states, unlabeled filters, and low-contrast text compound the failure.

**Casey, the distracted mobile user:** Some filters disappear outside the viewport, while important controls remain concentrated near the top. The rest of the touch-target sizing is comparatively strong.

**Riley, the experienced Pokémon collector:** The grid and completion feedback feel native, but the state channels require repeated rediscovery because no stable legend converts visual effects into learned meaning.

## Minor Observations

- Native collapsed selects for Type and Generation appropriately contain their large option sets.
- Width-based progress animations should use transforms if animation performance becomes visible.
- “Failed to remove — check console” should identify a recovery action rather than sending the user to developer tooling.
- The static side-border warning is outside the central Boxes flow and should not be treated as a priority from this critique.

## Questions to Consider

1. Should removal optimize for speed with a reversible undo, or for caution with a confirmation every time?
2. Should the state legend remain persistently visible, or open from a compact “Key” control near the filters?
3. Which filters deserve permanent mobile visibility, and which should move behind secondary disclosure?
4. Is keyboard operation part of the intended expert workflow, or should it be treated primarily as an accessibility path?
